import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#892cdc',
      contrastText: 'white',
    },
    secondary: {
      main: '#232931',
      contrastText: 'white',
    },
  },
});

export default theme;
